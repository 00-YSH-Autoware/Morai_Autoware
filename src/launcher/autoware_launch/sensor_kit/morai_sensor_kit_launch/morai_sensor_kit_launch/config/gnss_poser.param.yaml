/**:
  ros__parameters:
    base_frame: base_link
    gnss_base_frame: gnss_base_link
    map_frame: map
    buff_epoch: 1
    # MORAI 는 INS orientation 토픽(/autoware_orientation)을 주지 않으므로 false.
    # false 면 gnss_poser 가 연속 position 차이로 yaw 를 추정한다.
    use_gnss_ins_orientation: false
    gnss_pose_pub_method: 0
